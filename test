-- This script should be placed in StarterPlayerScripts or in a LocalScript in StarterPlayer > StarterPlayerScripts

-- Function to create and attach a box to a player's head
local function attachBoxToHead(player)
    -- Create a Box part
    local box = Instance.new("Part")
    box.Size = Vector3.new(2, 2, 2)  -- Adjust size as needed
    box.Shape = Enum.PartType.Block
    box.Color = Color3.fromRGB(255, 0, 0)  -- Red color, you can change this
    box.Anchored = true
    box.CanCollide = false
    
    -- Attach the box to the player's Head
    local head = player.Character and player.Character:FindFirstChild("Head")
    if head then
        box.CFrame = head.CFrame
        box.Parent = head
    end
end

-- Function to attach a box to the head of each player
local function attachBoxesToAllPlayers()
    for _, player in ipairs(game.Players:GetPlayers()) do
        attachBoxToHead(player)
    end
end

-- Attach boxes when a new player joins
game.Players.PlayerAdded:Connect(function(player)
    attachBoxToHead(player)
end)

-- Attach boxes to all existing players when the script runs
attachBoxesToAllPlayers()
